# Development Dockerfile для NestJS Backend
# Контекст сборки: корень monorepo (для npm workspaces)

FROM node:20-alpine

WORKDIR /app

# Копируем файлы конфигурации для npm workspaces
COPY package.json package-lock.json* ./
COPY apps/backend/package.json ./apps/backend/
COPY packages/shared/package.json ./packages/shared/

# Устанавливаем все зависимости (включая devDependencies для hot-reload)
RUN npm install

# Копируем и собираем shared пакет
COPY packages/shared/ ./packages/shared/
RUN npm run build --workspace=packages/shared

# Копируем исходники backend (будут перезаписаны volumes при запуске)
COPY apps/backend/ ./apps/backend/

EXPOSE 3001

# Запуск в development режиме с hot-reload
CMD ["npm", "run", "dev", "--workspace=apps/backend"]
